public enum Color2 {
  // enum values (objects)
  RED, BLUE; // create 2 objects - static final (全局唯一)

  // Normal Class Section
  // attributes (can be no attribute)
  // constructor
  // methods

  // the meaning of enum:
  // JVM will help create the Color2 Object (RED, BLUE)
  // As user, you cannot create the enum object by yourself
}